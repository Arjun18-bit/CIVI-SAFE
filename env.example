# MongoDB Configuration
# For local MongoDB (if installed)
MONGODB_URI=mongodb://localhost:27017/civisafe

# For MongoDB Atlas (cloud database) - uncomment and replace with your connection string
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/civisafe?retryWrites=true&w=majority

# Server Configuration
PORT=3001
NODE_ENV=development

# JWT Configuration
JWT_SECRET=civisafe-secret-key-2024

# File Upload Configuration
MAX_FILE_SIZE=10485760
UPLOAD_PATH=./uploads 